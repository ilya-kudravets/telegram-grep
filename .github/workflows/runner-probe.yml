name: Runner probe
on:
  push:
    branches: [docs/skill-audit]
jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        runs-on: [macos-15-intel, macos-14]
    runs-on: ${{ matrix.runs-on }}
    timeout-minutes: 5
    steps:
      - run: uname -sm
